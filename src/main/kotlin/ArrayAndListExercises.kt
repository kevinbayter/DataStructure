// Recibe un arreglo de enteros y retorna la suma de todos los elementos
fun sumArrayElements(arr: IntArray): Int {
    // TODO: Implementa la lógica para sumar los elementos del arreglo

    return 0
}

// Recibe una lista de cadenas y retorna la concatenación de todas ellas en un solo string
fun concatenateStrings(strings: List<String>): String {
    // TODO: Implementa la lógica para concatenar las cadenas de la lista


    return ""
}

fun main() {
    val arr = intArrayOf(1, 2, 3, 4, 5)
    val sum = sumArrayElements(arr)
    println("La suma de los elementos del arreglo es: $sum")

    // concatenateStrings
    val strings = listOf("Hola", "mundo", "!")
    val result = concatenateStrings(strings)
    println("La concatenación de las cadenas es: $result")
}
